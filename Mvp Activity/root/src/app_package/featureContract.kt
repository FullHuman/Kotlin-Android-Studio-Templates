package ${packageName};

interface ${moduleContactClass} {

    interface View : BaseView<Presenter> {
        // Functions like hideTitle()
    }

    interface Presenter : BasePresenter {
        // Functions like editTask()
    }
}
